# 업로드 폴더 보안 설정
# PHP 실행 방지
<FilesMatch "\.ph(p[3-5]?|tml)$">
    Deny from all
</FilesMatch>

# 허용된 이미지 확장자만
<FilesMatch "\.(jpg|jpeg|png|gif|webp)$">
    Allow from all
</FilesMatch>

# 디렉토리 리스팅 방지
Options -Indexes
